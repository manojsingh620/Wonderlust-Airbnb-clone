<style>
    .btn-search {
        display: flex;
        flex-direction: row;
        align-items: center;
        background-color: #FE424D;
        color: white;
        border-radius: 25px;
        padding: 0 1.2rem 0 1.2rem;
    }

    .btn-search:hover {
        background-color: #FE424D;
        color: white;
    }
    .btn-search i {
        display: inline;
        margin-right: 0.5rem;
    }

    .search-inp{
        border-radius: 25px;
        padding: 0.5rem 3rem 0.5rem 3rem;
    }

</style>

<nav class="navbar navbar-expand-md bg-light-yellow border-bottom sticky-top">
    <div class="container-fluid">
        <i class="fa-solid fa-compass-drafting navbar-brand"></i>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/listings">Explore</a>
            </div>
        </div>

        <div>
            <form class="d-flex">
                <input class="form-control me-2 search-inp"  placeholder="Search Destination" aria-label="Search">
                <button class="btn btn-search" type="submit">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    Search
                </button>
            </form>
        </div>

        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/listings/new">Add new listings</a>
                <%if(!currUser){%>
                    <a class="nav-link" href="/signup">signup</a>
                    <a class="nav-link" href="/login">Login</a>
                    <%}%>

                        <% if(currUser){ %>
                            <a class="nav-link" href="/logout">Logout</a>
                            <% } %>
            </div>
        </div>

    </div>
</nav>